<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/search"
        android:title="@string/search_title"
        android:icon="@android:drawable/ic_menu_search"
        android:showAsAction="collapseActionView|ifRoom"
        android:actionViewClass="android.widget.SearchView" />
    <item
        android:id="@+id/AnadirCoche"
        android:icon="@android:drawable/ic_menu_add"
        android:showAsAction="always"
        android:title="Añadir"></item>
    <item
        android:id="@+id/mainMenu"
        android:icon="@android:drawable/btn_default"
        android:showAsAction="always">

        <menu>
            <item
                android:id="@+id/AnadirCocheOverflow"
                android:icon="@android:drawable/ic_menu_add"
                android:showAsAction="never"
                android:title="Añadir"></item>
            <item
                android:id="@+id/BorrarTodas"
                android:icon="@android:drawable/ic_menu_delete"
                android:title="Borrar todos"></item>

            <item
                android:id="@+id/Cargarxml"
                android:icon="@android:drawable/ic_menu_rotate"
                android:showAsAction="never"
                android:title="Cargar XML"></item>
            <item
                android:id="@+id/apache"
                android:icon="@android:drawable/stat_notify_sync"
                android:showAsAction="never"
                android:title="Cargar de Apache"></item>
            <item
                android:id="@+id/Generarxml"
                android:icon="@android:drawable/ic_menu_set_as"
                android:showAsAction="never"
                android:title="Generar XML"></item>

        </menu>
    </item>
</menu>